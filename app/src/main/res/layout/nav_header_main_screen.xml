<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/confirm_ui_choose_service"
    android:background="@drawable/ic_banner_menu"
    android:gravity="top|center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/img_avatar"
        android:layout_width="@dimen/confirm_ui_button_option"
        android:layout_height="@dimen/confirm_ui_button_option"
        android:layout_centerHorizontal="true"
        android:src="@drawable/ic_avatar_default" />

    <ImageView
        android:id="@+id/img_edit_profile"
        android:layout_width="@dimen/size_icon_in_app"
        android:layout_height="@dimen/size_icon_in_app"
        android:layout_alignEnd="@+id/tv_profile_name"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/tv_profile_name"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:src="@drawable/ic_setting" />

    <TextView
        android:id="@+id/tv_profile_name"
        style="@style/CusTextViewTitleTheme"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/img_avatar"
        android:layout_marginTop="@dimen/confirm_ui_padding_w"
        android:gravity="center"
        android:text="Hoàng Hà"
        android:textStyle="normal"
        android:textColor="@android:color/white"
        android:textSize="@dimen/size_text_app_title" />


    <TextView
        android:id="@+id/tv_profile_des"
        style="@style/CusTextViewTitleTheme"
        android:layout_below="@+id/tv_profile_name"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="Thợ xuất sắc"
        android:textColor="@android:color/white"
        android:textSize="@dimen/size_text_app_title"
        android:textStyle="normal" />

    <ImageView
        android:id="@+id/img_rating"
        android:layout_width="@dimen/size_icon_in_app"
        android:layout_height="@dimen/size_icon_in_app"
        android:src="@drawable/ic_level_5"
        android:layout_alignTop="@+id/tv_profile_des"
        android:layout_toRightOf="@+id/tv_profile_des"
        android:layout_toEndOf="@+id/tv_profile_des" />


</RelativeLayout>
